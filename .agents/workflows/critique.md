# /critique

Apply the `agents/critique.md` persona to the artifact(s) below with the stated focus.
Read-only — report findings, do not fix anything yourself unless asked to in a follow-up.

Artifact(s) / focus: $ARGUMENTS

If no specific angle is given, infer the most relevant checklists (A-G in
`agents/critique.md`) from the artifact type and apply those. Return the full structured
report, including the "Verification passed" section.
